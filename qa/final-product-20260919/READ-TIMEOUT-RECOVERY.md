# Unacknowledged automatic quest-read recovery

Observed native freshready session1789834462-167381-1 with operationnull, while the actual desktop screenshot stayed unavailable. Mailbox contained its new-session DWQuestReadback.refresh command written12:19:57, deadline12:20:02. This was not the earlier independent difficulty timing request. Existing transport waited8seconds, then returned readyfalse indefinitely without releasing pending; the stale request could block all further dispatch in an otherwise connected game.

Changed only `electron/importedMenuTransport.ts` and its test file. Pending remembers whether it is the explicitly allowlisted read-only `invoke DWQuestReadback.refresh`. After8seconds, a fresh verified same-session snapshot with no native queued/running/confirmation operation can release that read and emit failed read feedback while keeping connection ready. The five-second command deadline has already elapsed. No automatic retry or setter replay occurs. Unknown/mutating requests keep the existing conservative block. A later acknowledgement for the old request cannot acknowledge a new pending request.

Regression first failed on readyfalse versus expectedreadytrue (`read-timeout-red.log`), then passed. Tests also cover mutation remaining blocked, unrelated native operation blocking read release, and old acknowledgement not consuming a subsequent mutation. Three focused files52tests passed. Lint and production build passed (existing largebundle warning). Full suite and package validation recorded separately when completed.

No Lua source, installed runtime, settings, saves or74file payload changed. Source backup: importedMenuTransport-before-read-recovery.ts.txt. Both edited code files retain executable cyberfox1337x signatures. Old release proof source/compiled bindings are intentionally stale until exact new build evidence is reviewed; do not silently reuse old production approval.

Parent preserved a native message naming ImportedMenuFacade.lua271: finite callbacks had to drain before another command. Inspection confirms Dispatch asserts pendingTotal==0 before creating the request-correlated operation. The caller already marks the mailbox request seen before dispatch, so this rejection can leave operationnull; that explains the observed startup read's missing acknowledgement. pendingFiniteTasks later reached0. Desktop snapshot contracts currently do not declare that field and request validation does not guard it. A separate conservative pre-dispatch busy check could reduce this race but cannot eliminate it; bounded read recovery remains necessary. No extra guard or Lua change was folded into this patch while its validation was running.

## Separately authorized finite-callback guard

After the timeout patch, parent approved the observed-busy pre-dispatch guard. `electron/importedMenuContract.ts` now declares optional pendingFiniteTasks; transport validates it as a nonnegative safe integer and refuses any dispatch while positive before writing command.txt. Missing counts remain backward-compatible. Invalid negative/fractional/NaN/Infinity/string/null cases fail closed. Seven new tests failed before implementation and pass after; a real App timer test proves a refused initialization read permits the next regular20second read and does not immediately replay it. `src/App.test.tsx` only adds that regression; renderer production code is unchanged. No Lua changes. This supersedes the earlier note that the guard was merely a follow-up idea.

Final verification after both changes: full npmtest635Vitest tests in71files and11Node checks passed; final lint/build passed; mandated release-current packaging passed.118compiled archive files match disk.222source/installed/packaged Lua payload hash/length checks pass,74files per root. Current app.asar SHA25692CC51C3FD148966E0160AA2B3C6728A677D4A7737C4854B438CAD4B42B08F71; executable SHA256F128B57976E0391D05F728424FCE47DE2A55CBF023AA383BB9AD9E16A73A3F9C. Live packaged verification remains parent-owned. Previous ledger bindings are rejected rather than silently approved for these source/build changes.

After the clean-startup comparison and restoring the original proxy, normal game PID19344 loaded positively selected Quicksave2 while the same normal menu PID18024 remained running. It automatically reconnected to fresh session1789836763-298873-1, displayed health/stamina and available toggles, and automatic quest read completed. Evidence: read-recovery-restart-connected.jpg/.json and native-profile-quicksave2-selected.jpg. This is real restart/reconnection proof; no missing acknowledgement occurred in this particular reload.
